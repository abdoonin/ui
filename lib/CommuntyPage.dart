import 'package:flutter/material.dart';

class CommuntyPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Communty Page'),
      ),
      body: Center(
        child: Text('This is the Communty page!'),
      ),
    );
  }
}
