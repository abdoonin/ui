import 'package:flutter/material.dart';

class ProgramsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Programs Page'),
      ),
      body: Center(
        child: Text('This is the programs page!'),
      ),
    );
  }
}
