import 'package:flutter/material.dart';

class ChallengesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Challenges Page'),
      ),
      body: Center(
        child: Text('This is the Challenges page!'),
      ),
    );
  }
}
